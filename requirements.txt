# --- Core RAG & LLM Framework ---
# Pinned versions are critical to avoid "langchain-core" conflicts
langchain==0.3.0
langchain-community==0.3.0
langchain-core==0.3.0
langchain-huggingface==0.1.0
langchain-groq==0.2.0

# --- Vector Database (CPU Version) ---
faiss-cpu

# --- Embeddings & Model Support ---
sentence-transformers

# --- Utilities ---
python-dotenv
pyyaml
